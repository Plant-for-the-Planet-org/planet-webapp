import React, { ReactElement } from 'react'

interface Props {
    
}

function ManageProjects({}: Props): ReactElement {
    return (
        <div>
            Manage Projects Home page
            we show a drop down for user to select a Project they have OR add another project
        </div>
    )
}

export default ManageProjects
