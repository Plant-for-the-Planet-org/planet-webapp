import React, { ReactElement } from 'react'

interface Props {
    
}

function ManageSingleProject({}: Props): ReactElement {
    return (
        <div>
            This page is for editing an already created project
        </div>
    )
}

export default ManageSingleProject
