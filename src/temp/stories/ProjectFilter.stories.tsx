import ProjectFilter from '../Filter/ProjectFilter';

const meta = {
  component: ProjectFilter,
};
export default meta;

export const filterStory = {
  args: {
    width: '12px',
    height: '12px',
  },
};
