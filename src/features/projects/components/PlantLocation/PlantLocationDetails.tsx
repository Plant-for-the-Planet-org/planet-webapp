import React, { ReactElement } from 'react';

interface Props {}

export default function PlantLocationDetails({}: Props): ReactElement {
  return (
    <div>
      <div>details</div>
    </div>
  );
}
