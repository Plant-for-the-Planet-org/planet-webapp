import { ReactElement } from 'react';
import StyledForm from '../../common/Layout/StyledForm';

const BankDetailsForm = (): ReactElement | null => {
  return <StyledForm>Bank Details</StyledForm>;
};

export default BankDetailsForm;
