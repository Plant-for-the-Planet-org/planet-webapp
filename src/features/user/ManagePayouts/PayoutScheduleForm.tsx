import { ReactElement } from 'react';
import StyledForm from '../../common/Layout/StyledForm';

const PayoutScheduleForm = (): ReactElement | null => {
  return <StyledForm>Payout Schedule</StyledForm>;
};

export default PayoutScheduleForm;
