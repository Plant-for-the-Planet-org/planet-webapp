import React, { ReactElement } from 'react'

interface Props {
    
}

export default function DetailedAnalysis({}: Props): ReactElement {
    return (
        <div>
            DetailedAnalysis
        </div>
    )
}
