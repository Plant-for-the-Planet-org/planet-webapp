import React, { ReactElement } from 'react'

interface Props {
    
}

export default function ProjectSpending({}: Props): ReactElement {
    return (
        <div>
            ProjectSpending
        </div>
    )
}
