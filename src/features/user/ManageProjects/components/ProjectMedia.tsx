import React, { ReactElement } from 'react'

interface Props {
    
}

export default function ProjectMedia({}: Props): ReactElement {
    return (
        <div>
            ProjectMedia
        </div>
    )
}
