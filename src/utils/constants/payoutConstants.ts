export enum PaymentFrequencies {
  MANUAL = 'manual',
  MONTHLY = 'monthly',
  QUARTERLY = 'quarterly',
  SEMIANNUAL = 'semiannually',
  ANNUAL = 'annually',
}

export enum PayoutCurrency {
  DEFAULT = 'default',
}
